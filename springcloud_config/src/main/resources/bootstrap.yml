spring:
  application:
    name: springcloud-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xyjspace/springcloud_config.git
          search-paths: springcloud/config_test
          username:
          password:

server:
  port: 8076
